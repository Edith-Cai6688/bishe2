dataset:
  repo_id: my_pi0 # Repository ID
  root: ./demo_data_language_2 # Your root for data file!
policy:
  type : pi0
  chunk_size: 5
  n_action_steps: 5
  
save_checkpoint: true
output_dir: ./ckpt/my_pi0 # Save directory
batch_size: 4
job_name : pi0_omy
resume: true 
seed : 42
num_workers: 8
steps: 20_000
eval_freq: -1 # No evaluation
log_freq: 50
save_checkpoint: true
save_freq: 10_000
use_policy_training_preset: true
  
wandb:
  enable: true
  project: my_pi0
  disable_artifact: true
